(define (a-plus-abs-b a b) ((if (> b 0) + -) a b))
;; Define a new method named "a-plus-abs-b", which takes two arguments, a and b. If b is greater than zero,
;; add a and b together. Otherwise, subtract b from a.

